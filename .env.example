# ============================================================================
# CONFIGURAÇÕES DO DJANGO - AMBIENTE DE PRODUÇÃO
# ============================================================================
# Este é um arquivo de EXEMPLO. Copie para .env e preencha com valores reais.
# NUNCA commite o arquivo .env no Git!
# ============================================================================

# -----------------------------------------------------------------------------
# SEGURANÇA
# -----------------------------------------------------------------------------
# SECRET_KEY: Chave secreta do Django (gere uma nova para produção!)
# Como gerar: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-MUDE-ISSO-EM-PRODUCAO

# DEBUG: Sempre False em produção!
DEBUG=False

# ALLOWED_HOSTS: Domínios permitidos (separados por vírgula, sem espaços)
# Exemplo: meusite.com,www.meusite.com,123.45.67.89
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------------------------
# BANCO DE DADOS - POSTGRESQL (PRODUÇÃO)
# -----------------------------------------------------------------------------
DB_ENGINE=django.db.backends.postgresql
DB_NAME=sisprof_db
DB_USER=sisprof_user
DB_PASSWORD=senha_super_segura_aqui
DB_HOST=localhost
DB_PORT=5432

# -----------------------------------------------------------------------------
# BANCO DE DADOS - SQLITE (DESENVOLVIMENTO - OPCIONAL)
# -----------------------------------------------------------------------------
# Para usar SQLite localmente, comente as variáveis PostgreSQL acima
# e descomente as linhas abaixo:
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# -----------------------------------------------------------------------------
# EMAIL (OPCIONAL - Para recuperação de senha, notificações)
# -----------------------------------------------------------------------------
# Gmail exemplo:
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-de-app-do-gmail

# MailHog (para testes locais - opcional):
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=localhost
# EMAIL_PORT=1025

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES ADICIONAIS (OPCIONAL)
# -----------------------------------------------------------------------------
# Timezone
TIME_ZONE=America/Manaus

# Idioma
LANGUAGE_CODE=pt-br

# URL do site (para links em emails)
SITE_URL=https://seu-dominio.com